This job is created to Parse the JSON data and load to Redshift tables. 

Running command:

/Users/tonys/PycharmProjects/shutterfly/src/clickstream_start.sh 2 input.txt
Please change the directory paths inside the script to run the scripts. The export variables needs to be changed to the project path before execution. 
